# Task Settings

Recommended task settings for Mux. These control sub-agent parallelism, nesting depth, and output compaction.

## Usage

These are reference values — apply them via the Mux settings UI or by editing `~/.mux/config.json` directly.

## Available Presets

| File | Description |
|------|-------------|
| `default.json` | Balanced defaults for most workflows |
